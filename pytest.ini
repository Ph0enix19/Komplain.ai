[pytest]
markers =
    requires_llm: tests that call a real LLM provider and require API keys
addopts = --strict-markers
pythonpath = .
testpaths = tests
norecursedirs = .git .pytest_cache .test-tmp test-tmp pytest-cache-files-*
